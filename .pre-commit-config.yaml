repos:
  - repo: local
    hooks:
      - id: check-changelog
        name: Vérifier CHANGELOG pour la version
        entry: python3 scripts/check_changelog.py
        language: system
        pass_filenames: false
        stages: [commit]
        always_run: true
